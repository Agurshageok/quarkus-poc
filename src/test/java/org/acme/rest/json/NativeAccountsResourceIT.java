package org.acme.rest.json;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeAccountsResourceIT extends AccountsResourceTest {

    // Execute the same tests but in native mode.
}